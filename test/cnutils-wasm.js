/**
 * WASM crypto namespace for cnutils integration tests (`#cnutils-wasm` import map).
 * Uses the same `test/wasm` web target as `test-crypto.js`.
 */
import * as wasmCrypto from "./wasm/crypto/crypto.js";

export default wasmCrypto;
